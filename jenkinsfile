pipeline {
    agent any
    stages {
		stage('Build') {
            steps{
                sh 'hostname'
            }
        }
        stage('Testing in remote nodes'){
            node('node3'){
                echo "deploying in nodes"
                sh 'hostname'
            }
        }
    }
}