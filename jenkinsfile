pipeline {
    agent {label 'Linuxservers'}
    stages {
		stage('Build') {
            steps{
                sh 'hostname'
            }
        }
        stage('Testing in remote nodes'){
            steps{
                echo "deploying in nodes"
                sh 'hostname'
            }
        }
    }
}